Vagrant.configure('2') do |config|
  config.vm.box = 'kaorimatz/fedora-21-x86_64'
  config.vm.define 'itamae-plugin-resource-filesystem' do |c|
    c.vm.provider :virtualbox do |v|
      v.name = 'itamae-plugin-resource-filesystem'
    end
  end
end
